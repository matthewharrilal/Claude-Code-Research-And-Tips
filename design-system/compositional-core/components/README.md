# Components Layer

**Files:**
- `components.css` — CSS implementations for all components (callouts, code blocks, tables, header/footer, bento grid, etc.)
- `components.md` — Component inventory with velocity (SLOW/MEDIUM/FAST) and weight (heavy/medium/light)

**Always-load:** `components.css` is mandatory reading. `components.md` available at Phase 4+.

**Key components:** Callout system (5 semantic types), code blocks, data tables, dark header + 3px red border, footer mirror, bento grid, file tree, section zones.

**Superseded files:** See `../_superseded/component-inventory.md`, `../_superseded/components-dd-od.css`, `../_superseded/components-cd-tt.css` for Phase C extraction originals.
