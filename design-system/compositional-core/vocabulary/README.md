# Vocabulary Layer

**Files:**
- `tokens.css` — 65 CSS custom properties in `:root` block (colors, typography, spacing, borders)

**Always-load:** This file is mandatory reading before any task.

**Key values:**
- Background: `#FEF9F5` (warm cream)
- Text: `#1A1A1A` (near-black)
- Primary: `#E83025` (editorial red)
- Fonts: Instrument Serif (display), Inter (body), JetBrains Mono (code)
- Spacing: 4px base unit
- Borders: 4px (structural), 3px (section), 1px (subtle)

**Superseded files:** See `../_superseded/tokens-mutability.md` for Phase C extraction original.
