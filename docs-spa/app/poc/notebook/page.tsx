'use client'

/**
 * Personal Notebook POC Demo Page
 *
 * Showcases the PersonalNotebook component that visualizes
 * documentation as someone's annotated field notebook.
 *
 * Design: Cream paper with blue lines, red margin, handwritten headers,
 * sticky notes for callouts, index cards for code blocks.
 */

import { PersonalNotebookDemo } from '@/components/ui-poc/PersonalNotebook'

export default function NotebookPOCPage() {
  return <PersonalNotebookDemo />
}
