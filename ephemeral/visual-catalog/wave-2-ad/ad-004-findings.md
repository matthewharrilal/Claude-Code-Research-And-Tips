# AD-004 Spiral — Visual Catalog Findings

**File:** `design-system/axis/AD-004-spiral.html`
**Lines:** 1484 (CSS: 70-963, HTML: 966-1484)
**Axis:** Spiral — angular approximation of golden-ratio spiral geometry
**Paired OD:** OD-004 Confidence (GEOLOGICAL density)
**Core concept:** Border-weight gradient (4px/3px/1px) as geological depth encoding; confidence-proportional spacing creates CRESCENDO whitespace

---

## INSTRUMENTS

### 1. Spiral Map — INSTRUMENT

**CSS class(es):** `.spiral-map`, `.spiral-map__stratum`, `.spiral-map__stratum--established`, `.spiral-map__stratum--probable`, `.spiral-map__stratum--speculative`, `.spiral-map__stratum--open`
**CSS lines:** 553-583
**HTML lines:** 989-994
**DOM structure:**
```html
<nav class="spiral-map" aria-label="Geological strata overview">
  <div class="spiral-map__stratum spiral-map__stratum--established">Bedrock</div>
  <div class="spiral-map__stratum spiral-map__stratum--probable">Subsoil</div>
  <div class="spiral-map__stratum spiral-map__stratum--speculative">Topsoil</div>
  <div class="spiral-map__stratum spiral-map__stratum--open">Atmos</div>
</nav>
```

**Reader function:** Miniature map in the header that previews all 4 confidence strata before reading begins. Orients the reader to the page's entire geological architecture — a table of contents encoded as a visual specimen rather than text.

**Detection reasoning:** This is a pure orientation device. It delivers zero article content; its only job is to show the reader WHERE they will travel (4 strata) and HOW MUCH confidence each stratum carries. Removing it eliminates pre-reading awareness of page structure. Instrument, not component, because its function is wayfinding, not content containment.

**Multi-channel coordination:** Three visual channels encode metadata simultaneously: (1) border-bottom weight varies per stratum (4px/3px/3px/1px), (2) border-bottom color shifts (primary red / border / border-subtle / border-subtle), (3) label text names geological layer (Bedrock/Subsoil/Topsoil/Atmos). The 4-column grid layout itself encodes equal structural status.

**Pipeline implication:** Generated by the question "How can the reader preview the entire confidence architecture before reading a single paragraph?" Requires awareness of the page's strata taxonomy and the border-weight encoding system.

**Derivation question:** "What if the header contained a miniature specimen showing all confidence layers at a glance, so the reader knows the geological structure before drilling down?"

---

### 2. Confidence Badge — INSTRUMENT

**CSS class(es):** `.confidence-badge`, `.confidence-badge--established`, `.confidence-badge--probable`, `.confidence-badge--speculative`, `.confidence-badge--open`
**CSS lines:** 678-703
**HTML lines:** 1017, 1132, 1255, 1330
**DOM structure:**
```html
<span class="confidence-badge confidence-badge--established">Confidence: High</span>
<span class="confidence-badge confidence-badge--probable">Confidence: Moderate</span>
<span class="confidence-badge confidence-badge--speculative">Confidence: Low</span>
<span class="confidence-badge confidence-badge--open">Confidence: Unresolved</span>
```

**Reader function:** Inline metadata chip at each stratum header that explicitly names the confidence level. Provides unambiguous certainty labeling — the reader never has to infer confidence from context alone.

**Detection reasoning:** The badge communicates metadata ABOUT the content (its confidence level), not the content itself. Removing it forces the reader to decode confidence solely from border weight and spacing — the explicit label is the redundant-but-critical human-readable channel. Instrument, not component, because it has no slots or reusable structure; it is a single-purpose label.

**Multi-channel coordination:** Border weight encodes confidence visually (3px solid primary for established, 3px solid border for probable, 1px solid border for speculative, 1px solid border-subtle for open). Border color also shifts across variants. Text label provides the semantic name. Together, three channels (weight, color, text) encode one datum: confidence level.

**Pipeline implication:** Generated by "How does the reader know the confidence level of each section without reading its content?" Requires a taxonomy of confidence levels and visual encoding rules.

**Derivation question:** "What if each section header carried a machine-readable confidence label that degraded visually in lockstep with the border-weight gradient?"

---

### 3. Stratum Transition Zone — INSTRUMENT

**CSS class(es):** `.stratum-transition`, `.stratum-transition__label`
**CSS lines:** 645-661
**HTML lines:** 1118-1120, 1240-1242, 1316-1318
**DOM structure:**
```html
<div class="stratum-transition" role="separator" aria-label="Transition from Established to Probable">
  <span class="stratum-transition__label">Descending: Bedrock &rarr; Subsoil</span>
</div>
```

**Reader function:** Explicit wayfinding marker between strata that names the transition direction ("Descending: Bedrock -> Subsoil"). Acts as a cognitive decompression zone — a breathing pause that signals the reader is crossing from one confidence level to another.

**Detection reasoning:** This element carries zero content. Its sole function is reader orientation: announcing a boundary crossing between confidence levels. The 48px height + breathing background color create a perceptual pause. Removing it would cause strata to visually merge, destroying the geological layering metaphor. Instrument because it is a wayfinding device, not a content container.

**Multi-channel coordination:** Four channels: (1) 48px height creates physical pause, (2) 3px top border marks the structural boundary, (3) breathing zone background color (#FAF5ED) signals liminal space, (4) monospace uppercase label names the transition direction and geological metaphor ("Descending: Bedrock -> Subsoil").

**Pipeline implication:** Generated by "How does the reader know they are crossing from one confidence level to another?" Requires geological layer naming and directional metaphor.

**Derivation question:** "What if the transitions between confidence zones were explicitly named, creating cognitive rest stops that let the reader recalibrate their reading register?"

---

### 4. Metadata Panel — INSTRUMENT

**CSS class(es):** `.metadata-panel`, `.metadata-panel__title`, `.metadata-panel__item`, `.metadata-panel__key`, `.metadata-panel__value`
**CSS lines:** 756-790
**HTML lines:** 1081-1103
**DOM structure:**
```html
<div class="metadata-panel">
  <div class="metadata-panel__title">Stratum Metadata</div>
  <div class="metadata-panel__item">
    <span class="metadata-panel__key">Confidence</span>
    <span class="metadata-panel__value">HIGH</span>
  </div>
  <div class="metadata-panel__item">
    <span class="metadata-panel__key">Border</span>
    <span class="metadata-panel__value">4px</span>
  </div>
  <!-- ...more items... -->
</div>
```

**Reader function:** Sidebar instrument that surfaces quantitative metadata about the stratum: confidence level, border weight, evidence count, stability status, version range. Makes the stratum's structural properties visible as data rather than implicit visual cues.

**Detection reasoning:** Every data point in this panel is ABOUT the stratum, not article content. It answers "what are the properties of this confidence layer?" — pure meta-awareness. The golden-ratio sidebar placement (0.618fr) ensures it is always visible alongside content without competing with it. Instrument because it exposes structural metadata; its removal would eliminate explicit quantitative awareness.

**Multi-channel coordination:** Key-value layout with monospace font for data fidelity. 3px structural border, 1px subtle separators between items. Title uses uppercase + letter-spacing to signal "metadata zone." Values in font-weight 500 for emphasis.

**Pipeline implication:** Generated by "What if each stratum made its own structural properties explicit — confidence, evidence, stability — as a sidebar reference card?"

**Derivation question:** "What if the most information-dense stratum had a companion panel showing its vital statistics, so the reader could assess credibility without leaving the content flow?"

---

### 5. Meta Line — INSTRUMENT

**CSS class(es):** `.meta-line`
**CSS lines:** 257-267
**HTML lines:** 975-979
**DOM structure:**
```html
<div class="meta-line">
  <span>EXPLORATION AD-004</span>
  <span>AXIS: SPIRAL</span>
  <span class="version-badge">v1 enriched</span>
</div>
```

**Reader function:** Top-of-header identification line that names the exploration ID, axis type, and version. Orients the reader to what document they are reading within the larger AD exploration sequence.

**Detection reasoning:** Pure identification metadata — not article content. Tells the reader WHERE this document fits in the exploration taxonomy. Removing it would eliminate document-level orientation. Instrument because it is metadata labeling, not content delivery.

**Multi-channel coordination:** Uppercase + letter-spacing (0.15em) + meta font size (0.75rem) + secondary color + flex layout with gap. All channels signal "this is metadata, not content."

**Pipeline implication:** Generated by "How does the reader know which exploration they are reading and where it fits in the sequence?"

**Derivation question:** "What if the header's first line was a systematic identifier that placed this document within the exploration taxonomy?"

---

### 6. Version Badge — INSTRUMENT

**CSS class(es):** `.version-badge`
**CSS lines:** 269-278
**HTML lines:** 978
**DOM structure:**
```html
<span class="version-badge">v1 enriched</span>
```

**Reader function:** Inline badge in the meta line that communicates the document's revision state. Tells the reader whether they are reading a draft, v1, or enriched version.

**Detection reasoning:** Single-datum metadata chip. Its removal would eliminate version awareness. Instrument because it serves document-level orientation, not content delivery.

**Multi-channel coordination:** Monospace font signals "literal data." 1px border (Cat 3 micro) + secondary color = low-emphasis metadata. Padding 2px/8px creates compact pill shape.

**Pipeline implication:** Generated by "How does the reader know which version/revision of this exploration they are reading?"

**Derivation question:** "What if the document's version state was visible as a compact badge, so the reader could immediately assess currency?"

---

### 7. Research Application Record (RAR) — INSTRUMENT

**CSS class(es):** `.rar-block`, `.rar-block__title`
**CSS lines:** 912-962
**HTML lines:** 1390-1427
**DOM structure:**
```html
<div class="rar-block">
  <div class="rar-block__title">Research Application Record</div>
  <dl>
    <dt>1. Exploration ID</dt>
    <dd>AD-004 Spiral</dd>
    <dt>2. Axis Pattern</dt>
    <dd>Spiral — angular approximation...</dd>
    <!-- ...9 more fields... -->
  </dl>
</div>
```

**Reader function:** Structured provenance record that maps the exploration back to its research citations, findings, soul compliance status, and 3-way unification hypothesis result. Makes the exploration's scholarly apparatus visible.

**Detection reasoning:** This is entirely metadata ABOUT the exploration — citation counts, finding IDs, compliance status. It does not present article content; it presents the exploration's research DNA. Instrument because its function is provenance awareness, not content delivery.

**Multi-channel coordination:** Definition list (dt/dd) enforces key-value structure. 3px structural border on title. Monospace for dt keys, body font for dd values. Sparse background isolates it from content sections.

**Pipeline implication:** Generated by "How does the reader verify the research backing and compliance status of this exploration?"

**Derivation question:** "What if every exploration ended with a structured record that traced every design decision back to its research citation, so provenance was never opaque?"

---

### 8. Page Footer — INSTRUMENT

**CSS class(es):** `.page-footer`, `.page-footer__label`
**CSS lines:** 921-935
**HTML lines:** 1429-1431
**DOM structure:**
```html
<div class="page-footer" role="contentinfo">
  <span class="page-footer__label">End of Exploration AD-004 — Spiral Axis — Bedrock to Atmosphere</span>
</div>
```

**Reader function:** Explicit visual closure that signals "you have reached the end." Names the exploration and its geological range (Bedrock to Atmosphere), confirming the reader completed the full spiral traversal.

**Detection reasoning:** Zero content — pure terminal wayfinding. Without it, the page ends ambiguously after the RAR block. Its text ("Bedrock to Atmosphere") confirms the reader traversed all 4 strata. Instrument because it serves reading-position awareness.

**Multi-channel coordination:** 3px top border (Cat 1 structural) creates visual boundary. Monospace font + uppercase + letter-spacing signal "system metadata." Centered text + margin-top creates a terminal breathing zone.

**Pipeline implication:** Generated by "How does the reader know they have reached the end of the exploration?"

**Derivation question:** "What if the page closed with an explicit label that named the journey just completed — from bedrock to atmosphere?"

---

## GESTURES

### 9. Border-Weight Confidence Gradient — GESTURE

**CSS class(es):** `.stratum--established`, `.stratum--probable`, `.stratum--speculative`, `.stratum--open`
**CSS lines:** 608-642
**HTML lines:** 1014, 1129, 1252, 1327 (each section tag)

**Reader function:** A four-level border-weight gradient that encodes confidence as physical thickness. The reader's eye registers decreasing visual weight (4px -> 3px -> 3px -> 1px) as decreasing certainty. This is the exploration's primary semantic encoding — border weight IS the meaning, not decoration.

**Detection reasoning:** This is not a single element but a coordinated CSS shift across 4 strata. Multiple properties change together according to one rule: "visual weight correlates with confidence." Gesture because it shifts the reader's perceptual register across sections, not because it contains content.

**Multi-channel coordination:** Four CSS properties shift in lockstep across strata:
- `border-left-width`: 4px -> 3px -> 3px -> 1px (weight = confidence)
- `border-left-color`: primary red -> border -> border-subtle -> border-subtle (color saturation = confidence)
- `padding-left`: space-8 (32px) -> space-10 (40px) -> space-12 (48px) -> space-16 (64px) (increasing indent = decreasing confidence, spiral outward)
- `background`: zone-dense (#FFF) -> zone-sparse (#FEF9F5) -> zone-breathing (#FAF5ED) -> background (#FEF9F5) (warmth/density shift)

All four channels encode ONE datum: confidence level. This is the core gesture of AD-004.

**Pipeline implication:** Generated by "What if the reader could feel confidence degrading through the physical properties of the container — without reading a single word?"

**Derivation question:** "What if border weight, spacing, and background all varied together to encode certainty — so the visual substrate itself communicates how much to trust the content?"

---

### 10. Confidence-Proportional Spacing (CRESCENDO Whitespace) — GESTURE

**CSS class(es):** `.stratum--established` through `.stratum--open` (padding values)
**CSS lines:** 608-642
**HTML lines:** 1014, 1129, 1252, 1327

**Reader function:** Whitespace expands progressively from core (tightest) to periphery (most generous). Dense content = certainty; generous space = uncertainty. The amount of whitespace IS the confidence signal — not just a layout choice, but a semantic channel.

**Detection reasoning:** This is a gesture because it governs the reader's pace. Dense, tight spacing in the Established stratum creates fast, authoritative reading. Expanding whitespace in later strata slows the reader and communicates "tread carefully." The CSS shift is coordinated: padding-top, padding-bottom, and padding-left all expand together across strata.

**Multi-channel coordination:**
- Established: `padding: 24px 24px 32px 32px` (tight)
- Probable: `padding: 32px 24px 32px 40px` (moderate)
- Speculative: `padding: 32px 24px 32px 48px` (generous)
- Open: `padding: 32px 24px 32px 64px` (vast)

The left-padding specifically encodes the spiral's outward expansion: 32px -> 40px -> 48px -> 64px. This is both CRESCENDO whitespace (DD-F-002) and spiral geometry rendered as indent depth.

**Pipeline implication:** Generated by "What if the spacing itself encoded confidence — tight for certain, expansive for uncertain?"

**Derivation question:** "What if moving from bedrock to atmosphere felt physically different — tighter packing for facts, wider breathing room for speculation?"

---

### 11. Background Zone Shift — GESTURE

**CSS class(es):** `.stratum--established` through `.stratum--open` (background values)
**CSS lines:** 612, 621, 631, 641

**Reader function:** Background color shifts subtly across strata: dense white (#FFFFFF) -> sparse cream (#FEF9F5) -> breathing warm (#FAF5ED) -> background cream (#FEF9F5). The shift is intentionally subtle — it registers subconsciously, adding to the cumulative confidence signal without demanding attention.

**Detection reasoning:** Gesture because it is a coordinated shift across sections that changes the reader's ambient perception. Not a component (no structure) and not an instrument (no explicit metadata). The reader does not consciously read the background color, but the shift from clinical white (established) to warm earth tones (speculative/open) shifts the emotional register.

**Multi-channel coordination:** Background works in concert with border-weight and spacing — all three shift together. The zone token naming (dense/sparse/breathing/background) explicitly maps background colors to geological metaphor.

**Pipeline implication:** Generated by "What if the background itself carried confidence information — clinical for facts, warmer for speculation?"

**Derivation question:** "What if moving through confidence layers felt like moving through geological zones — cool dense rock at the core, warm airy atmosphere at the edges?"

---

### 12. Typography Scale Degradation — GESTURE

**CSS class(es):** Implicit in stratum header hierarchy — h2 `.stratum__title` remains consistent, but the overall typographic register shifts (OD-F-010 cited)
**CSS lines:** 665-674 (stratum header), 186-190 (h2/h3 sizes)
**HTML lines:** 1016, 1131, 1254, 1329 (h2 titles), 1038, 1151, 1208, 1267, 1348 (h3 subsections)

**Reader function:** While the h2 stratum titles maintain consistent size (type-section: 1.625rem), the amount of content and subsection density decreases from Established to Open. Established has the most h3 subsections and the densest body text; Open has fewer headings and sparser content. The progressive thinning of typographic density mirrors the confidence gradient.

**Detection reasoning:** Gesture because it shifts reading register — the Established stratum reads as dense technical documentation while the Open stratum reads as exploratory musing. The shift is not in CSS properties per se but in the coordination between typographic density and the confidence encoding.

**Multi-channel coordination:** Content density (number of headings, paragraphs, code blocks) + spacing + border weight all decrease together. The h3 italic style (line 189) provides rhythmic punctuation within strata that becomes less frequent as confidence decreases.

**Pipeline implication:** Generated by "What if typographic density — the amount of text per unit area — itself encoded confidence?"

**Derivation question:** "What if the most confident content was also the most densely typeset, and the least confident content had the most breathing room?"

---

### 13. Pull Quote Confidence Modulation — GESTURE

**CSS class(es):** `.pull-quote` with per-instance color override
**CSS lines:** 731-753
**HTML lines:** 1136-1140 (Probable stratum), 1334-1338 (Open stratum)

**Reader function:** Pull quotes appear at stratum waypoints as confidence-annotated summaries. The default pull quote uses the primary red border-left (4px), but the Open stratum's pull quote overrides to amber (`border-left-color: var(--accent-amber)`), signaling reduced confidence through color shift.

**Detection reasoning:** Gesture because the same component (pull quote) changes its visual register across strata. The color shift from red to amber is a coordinated signal: red = structural certainty, amber = caution/uncertainty. This is not just a style variation — it changes how the reader interprets the quote's authority.

**Multi-channel coordination:** Border-left color shifts from primary red (line 737, default) to amber (line 1334, inline override). The attribution text also shifts from "Subsoil stratum confidence marker" to "Open stratum confidence marker" — the label names the confidence context.

**Pipeline implication:** Generated by "What if pull quotes visually degraded in authority across strata — so the reader could see confidence level without reading the attribution?"

**Derivation question:** "What if the same typographic device changed its visual authority based on where it appeared in the confidence hierarchy?"

---

### 14. Open Question Mark Prefix — GESTURE

**CSS class(es):** `.open-questions li::before`
**CSS lines:** 846-856
**HTML lines:** 1350-1371

**Reader function:** Each open question is prefixed with a large serif italic "?" character (type-section size: 1.625rem), rendered in secondary gray. This shifts the reader's mode from absorbing information to formulating questions — the typography signals "this is unanswered" before the reader processes the text.

**Detection reasoning:** Gesture because it shifts reading register from declarative to interrogative. The pseudo-element is not a component (no reusable container) and not an instrument (no metadata). It is a typographic inflection point that changes HOW the reader engages with what follows.

**Multi-channel coordination:** Instrument Serif font (display family) + italic + section-size (1.625rem) + secondary color + 32px fixed width creates a substantial typographic marker. The hanging indent (text-indent negative + padding-left positive) creates a "? ... question text" layout where the question mark anchors the left margin.

**Pipeline implication:** Generated by "How does the reader's mode shift from 'absorbing facts' to 'considering open questions'?"

**Derivation question:** "What if unresolved questions were introduced by a typographic sentinel — a large, unmistakable '?' that shifts the reader from certainty to inquiry?"

---

### 15. Responsive Spiral Collapse — GESTURE

**CSS class(es):** `@media (max-width: 769px)` rules
**CSS lines:** 861-910
**HTML lines:** N/A (media query, affects layout at 768px and below)

**Reader function:** At narrow viewports, the golden-ratio two-column layout collapses to single column, the spiral map rearranges to 2x2 grid, and left-padding on deeper strata reduces (but maintains the progressive indent gradient: space-6 -> space-8 -> space-10). The confidence encoding survives the collapse — it adapts rather than disappears.

**Detection reasoning:** Gesture because the responsive breakpoint is a coordinated multi-property shift that changes reading mode. The collapse from two-column to single-column changes the Established stratum's reading pattern from "scan content and sidebar simultaneously" to "read content then sidebar sequentially." The reduced padding preserves the spiral-outward indent gradient at smaller scale.

**Multi-channel coordination:** Six CSS properties shift together at 769px: header padding, page-container padding, content-grid columns, comparison-grid columns, spiral-map columns, stratum left-padding (3 strata). All coordinated to preserve the spiral's confidence encoding at narrow viewports.

**Pipeline implication:** Generated by "How does the spiral's confidence encoding survive viewport collapse?"

**Derivation question:** "What if the spiral's semantic encoding persisted at mobile width — the indent gradient compressed but not eliminated, so confidence remains visible?"

---

## COMPONENTS

### 16. Stratum — COMPONENT

**CSS class(es):** `.stratum`, `.stratum--established`, `.stratum--probable`, `.stratum--speculative`, `.stratum--open`, `.stratum__header`, `.stratum__title`, `.stratum__content-grid`, `.stratum__main`, `.stratum__sidebar`
**CSS lines:** 601-642, 665-727
**HTML lines:** 1014-1115 (Established), 1129-1237 (Probable), 1252-1313 (Speculative), 1327-1382 (Open)
**DOM structure:**
```html
<section aria-label="Established — Bedrock Stratum" class="stratum stratum--established">
  <div class="stratum__header">
    <h2 class="stratum__title">Established</h2>
    <span class="confidence-badge confidence-badge--established">Confidence: High</span>
  </div>
  <div class="stratum__content-grid">
    <div class="stratum__main">
      <!-- Content: paragraphs, code blocks, h3 subsections -->
    </div>
    <aside class="stratum__sidebar">
      <!-- Metadata panel, callouts -->
    </aside>
  </div>
</section>
```

**Reader function:** Multi-slot confidence-encoded container that holds one geological layer's content. Provides defined slots: header (title + badge), content grid (main + optional sidebar). The variant modifier (--established through --open) applies the confidence encoding via the border-weight gradient gesture.

**Detection reasoning:** Component because it is a reusable container with defined slots and consistent structure. All 4 strata use the same DOM pattern. The variant modifiers apply different visual treatments but the structural skeleton is identical.

**Multi-channel coordination:** The stratum's visual treatment is the sum of its gestures: border-weight, padding, background, and typography density all coordinated by the confidence modifier. The content grid optionally adds a golden-ratio sidebar (only in Established).

**Pipeline implication:** Generated by "What container structure can hold one confidence layer's content while encoding its certainty level through visual properties?"

**Derivation question:** "What if each geological layer was a structured section with header, content grid, and optional sidebar — where the container itself communicates confidence?"

---

### 17. Spiral Container — COMPONENT

**CSS class(es):** `.spiral-container`
**CSS lines:** 590-594
**HTML lines:** Implicit — the `.page-container` holds all strata in sequence
**DOM structure:**
```html
<div class="page-container">
  <section class="stratum stratum--established">...</section>
  <div class="stratum-transition">...</div>
  <section class="stratum stratum--probable">...</section>
  <div class="stratum-transition">...</div>
  <section class="stratum stratum--speculative">...</section>
  <div class="stratum-transition">...</div>
  <section class="stratum stratum--open">...</section>
</div>
```

**Reader function:** The outermost layout container that arranges strata in vertical sequence (flex column, gap 0). Ensures strata and transition zones stack without unwanted gaps, creating the angular spiral's sequential reading order.

**Detection reasoning:** Component because it defines the overall layout structure — the container within which strata and transitions are arranged. Without it, the strata would not compose into a coherent spiral reading experience.

**Multi-channel coordination:** Flex column layout + gap: 0 ensures continuous geological layering. The strata and transitions alternate in DOM order, creating the spiral's downward traversal.

**Pipeline implication:** Generated by "What container arranges geological strata into a coherent downward spiral?"

**Derivation question:** "What if the strata stacked vertically with zero gap, creating a continuous geological column where transitions marked boundary crossings?"

---

### 18. Comparison Grid — COMPONENT

**CSS class(es):** `.comparison-grid`, `.comparison-grid__panel`, `.comparison-grid__panel--established`, `.comparison-grid__panel--speculative`, `.comparison-grid__title`
**CSS lines:** 798-828
**HTML lines:** 1275-1300
**DOM structure:**
```html
<div class="comparison-grid">
  <div class="comparison-grid__panel comparison-grid__panel--established">
    <div class="comparison-grid__title">Established Approach</div>
    <p>...</p>
    <pre><code>...</code></pre>
  </div>
  <div class="comparison-grid__panel comparison-grid__panel--speculative">
    <div class="comparison-grid__title">Speculative Approach</div>
    <p>...</p>
    <pre><code>...</code></pre>
  </div>
</div>
```

**Reader function:** Side-by-side comparison container that juxtaposes established vs speculative approaches. Allows direct visual comparison within the Speculative stratum, demonstrating how the same content would be documented at different confidence levels.

**Detection reasoning:** Component because it is a reusable two-panel layout with defined slots (title + content per panel). The variant modifiers (--established, --speculative) apply different visual treatments (left border color: primary red vs amber) to each panel.

**Multi-channel coordination:** 2-column grid (1fr 1fr) + 3px structural border + confidence-encoded left border (4px primary for established, 4px amber for speculative) + background zone shift (dense white vs breathing warm).

**Pipeline implication:** Generated by "How can two confidence levels be compared side-by-side within a single stratum?"

**Derivation question:** "What if the Speculative stratum contained a direct comparison showing how the same API surface reads differently at established vs speculative confidence?"

---

### 19. Pull Quote — COMPONENT

**CSS class(es):** `.pull-quote`, `.pull-quote__attribution`
**CSS lines:** 731-753
**HTML lines:** 1136-1140, 1334-1338
**DOM structure:**
```html
<div class="pull-quote">
  These patterns have emerged consistently across multiple implementations
  but have not yet survived a full adversarial audit cycle.
  <span class="pull-quote__attribution">Subsoil stratum confidence marker</span>
</div>
```

**Reader function:** Two-slot content container: a serif italic quotation + a monospace uppercase attribution. Structurally anchors key summarizing statements at spiral waypoints (stratum entries).

**Detection reasoning:** Component because it is a reusable container with defined slots (quote body + attribution). Used across multiple strata with the same DOM structure. The confidence modulation (gesture #13) is applied ON TOP of this component.

**Multi-channel coordination:** Display font (Instrument Serif) + italic + section-size (1.625rem) + 4px primary left border + sparse background. Attribution uses body font, meta size, uppercase + letter-spacing.

**Pipeline implication:** Generated by "What container can hold a summarizing quotation at stratum entry points?"

**Derivation question:** "What if each stratum opened with a pull quote that summarized the confidence character of what follows?"

---

### 20. Callout — COMPONENT

**CSS class(es):** `.callout`, `.callout--essence`, `.callout--caution`, `.callout--note`, `.callout--tip`, `.callout__label`, `.callout__body`
**CSS lines:** 350-392
**HTML lines:** 1106-1112 (essence), 1197-1204 (caution), 1303-1312 (note), 1373-1381 (tip)
**DOM structure:**
```html
<div class="callout callout--essence">
  <div class="callout__label">Essence</div>
  <div class="callout__body">
    The border's weight is not decoration — it is the stratum itself,
    compressed into a single flat signal. Four pixels of certainty.
  </div>
</div>
```

**Reader function:** Two-zone container (label + body) for sidebar commentary, caveats, findings, and forward threads. Four variants used across the page: essence (purple, serif italic body), caution (amber), note (blue), tip (green).

**Detection reasoning:** Component because it is a reusable container with defined slots (label + body) and consistent visual treatment. 4 instances across the page, each in a different accent variant.

**Multi-channel coordination:** 4px left border (accent color per variant) + label (uppercase, colored, meta-size) + body (varies: display serif italic for essence, body font for others). The 5-variant system (note/tip/gotcha/caution/essence) is a complete callout family.

**Pipeline implication:** Generated by "What container can hold editorial commentary, caveats, and findings without interrupting the main content flow?"

**Derivation question:** "What if editorial sidebars had a 5-variant accent system where border color and label color encoded the commentary type?"

---

### 21. Open Questions List — COMPONENT

**CSS class(es):** `.open-questions`, `.open-questions li`, `.open-questions li::before`
**CSS lines:** 831-856
**HTML lines:** 1350-1371
**DOM structure:**
```html
<ul class="open-questions">
  <li>
    Does the angular spiral approximation lose essential properties...
  </li>
  <li>
    Can the border-weight gradient be extended beyond four levels...
  </li>
  <!-- ...more items... -->
</ul>
```

**Reader function:** Structured list container for unresolved questions in the Open stratum. Each item is prefixed with a large serif italic "?" and separated by subtle bottom borders.

**Detection reasoning:** Component because it is a reusable list structure with consistent per-item treatment (padding, border-bottom, hanging indent, pseudo-element prefix). The `::before` pseudo-element constitutes an embedded gesture (see gesture #14), but the overall list structure is a component.

**Multi-channel coordination:** No-bullet list + hanging indent (40px padding-left, -40px text-indent) + 1px subtle separator + serif italic "?" prefix at section-size. The combination creates a reading rhythm where each question starts with a visual sentinel.

**Pipeline implication:** Generated by "What container structure is appropriate for unresolved questions in the least-confident stratum?"

**Derivation question:** "What if open questions had their own list structure — not a standard bullet list, but something that visually signaled 'these are unanswered'?"

---

### 22. Header — COMPONENT

**CSS class(es):** `header`, `.header-inner`
**CSS lines:** 244-295
**HTML lines:** 973-996
**DOM structure:**
```html
<header>
  <div class="header-inner">
    <div class="meta-line">
      <span>EXPLORATION AD-004</span>
      <span>AXIS: SPIRAL</span>
      <span class="version-badge">v1 enriched</span>
    </div>
    <h1>AD-004: Spiral Layout</h1>
    <p class="subtitle">...</p>
    <nav class="spiral-map">...</nav>
  </div>
</header>
```

**Reader function:** Full-bleed dark header that contains the meta-line, title, subtitle, and spiral map. Establishes the exploration's identity and provides initial orientation before any content.

**Detection reasoning:** Component because it is a multi-slot container with defined structure: meta-line, h1 title, subtitle paragraph, and spiral map nav. Consistent pattern across all AD explorations.

**Multi-channel coordination:** Dark background (#1A1A1A) + 3px primary red bottom border + light text (#FEF9F5). Max-width 1100px inner container. Multiple nested instruments (meta-line, version badge, spiral map) within the component.

**Pipeline implication:** Generated by "What structure orients the reader before they enter the spiral content?"

**Derivation question:** "What if the header was a dark establishing shot that named the exploration, previewed its structure, and set the reading context?"

---

### 23. Table — COMPONENT

**CSS class(es):** `table`, `th`, `td`
**CSS lines:** 397-429
**HTML lines:** 1159-1194
**DOM structure:**
```html
<table>
  <thead>
    <tr>
      <th>Pattern</th>
      <th>Implementations</th>
      <th>Success Rate</th>
      <th>Confidence</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Border-weight encoding</td>
      <td>4 projects</td>
      <td>92%</td>
      <td>Probable</td>
    </tr>
    <!-- ...more rows... -->
  </tbody>
</table>
```

**Reader function:** Open-grid data table (no vertical borders, "anti-prison-bars" pattern) for structured data comparison within strata. Used in the Probable stratum to present pattern success rates.

**Detection reasoning:** Component because it is a reusable content container with defined structure (thead/tbody, th/td). The styling enforces the design system's table conventions: no vertical borders, uppercase column headers, subtle row separators.

**Multi-channel coordination:** 3px structural bottom border on th (Cat 1) + 1px subtle separator on td (Cat 2). Uppercase + letter-spacing + meta-size on headers. Body font at body-size on cells.

**Pipeline implication:** Generated by "What container presents structured data within a stratum without violating the anti-prison-bars convention?"

**Derivation question:** "What if tabular data used only horizontal separators — no vertical borders — so the data breathed laterally?"

---

### 24. Code Block — COMPONENT

**CSS class(es):** `pre`, `pre code`, syntax highlighting classes (`.kw`, `.st`, `.cm`, `.fn`, `.ty`, `.nu`, `.pu`)
**CSS lines:** 309-335
**HTML lines:** 1047-1066, 1068-1076, 1217-1236, 1283-1285, 1295-1298
**DOM structure:**
```html
<pre><code><span class="cm">/* Spiral strata via border-weight gradient */</span>
<span class="kw">.stratum--established</span> <span class="pu">{</span>
  <span class="fn">border-left</span><span class="pu">:</span> <span class="nu">4px</span> ...
<span class="pu">}</span></code></pre>
```

**Reader function:** Dark-background code container with 7-token syntax highlighting. Used to demonstrate CSS implementations within strata. The dark background creates a visual zone shift from the stratum's content zone.

**Detection reasoning:** Component because it is a reusable container with defined structure (pre > code > spans with syntax classes). 5 instances across the page. Consistent visual treatment: dark background, monospace font, 3px structural border, syntax highlighting.

**Multi-channel coordination:** Dark background (#1A1A1A) + light text (#FEF9F5) + 3px structural border + 7 syntax colors (keyword red, string green, comment gray, function blue, type coral, number amber, punctuation border). Monospace font at code-size (0.875rem).

**Pipeline implication:** Generated by "What container presents code examples within the geological strata?"

**Derivation question:** "What if code blocks were dark islands within the light content flow — visually distinct zones that signal 'this is literal implementation'?"

---

### 25. Inline Code — COMPONENT

**CSS class(es):** `code:not(pre code)`
**CSS lines:** 338-344
**HTML lines:** Throughout (1032, 1033, 1145, 1146, 1211, 1212, 1213, 1214, 1342, 1358, etc.)
**DOM structure:**
```html
<code>4px</code>
<code>3px</code>
<code>768px</code>
```

**Reader function:** Inline monospace fragment that marks literal values (pixel measurements, function names, CSS properties) within prose. Subtle background (#F0EBE3) and 1px border differentiate it from surrounding body text.

**Detection reasoning:** Component because it is a reusable inline container with consistent visual treatment. Many instances across the page. Small but structurally defined: monospace font, reduced size (0.9em), background, padding, border.

**Multi-channel coordination:** Monospace font signals "literal." Subtle background + 1px micro border (Cat 3) differentiate from prose. 0.9em size creates slight visual compression.

**Pipeline implication:** Generated by "How are literal values (measurements, code tokens) distinguished from prose?"

**Derivation question:** "What if inline technical values were monospace with a subtle background, so the reader immediately recognized 'this is a literal value'?"

---

## ADDITIONAL OBSERVATIONS

### 26. Selection Styling — GESTURE (micro)

**CSS class(es):** `::selection`
**CSS lines:** 209-212

**Reader function:** Text selection uses primary red background with cream text, branding the selection interaction itself.

**Detection reasoning:** Micro-gesture — shifts the reader's interaction register when selecting text. The primary red reinforces brand identity even during incidental interactions.

**Multi-channel coordination:** Background (primary red) + color (background cream) — two channels encode brand identity.

**Pipeline implication:** "What if even text selection reflected the design system's identity?"

**Derivation question:** "What if the selection highlight was branded — so copy-pasting felt like interacting with the system, not just the browser?"

---

### 27. Callout Essence Serif Voice — GESTURE (embedded in component)

**CSS class(es):** `.callout--essence .callout__body`
**CSS lines:** 389-392
**HTML lines:** 1108-1111

**Reader function:** The essence callout variant shifts its body text from Inter (body font) to Instrument Serif italic. This is a font-family shift — the "Archivist speaks in serif" voice that breaks the body text's typographic register to signal philosophical/identity-level content.

**Detection reasoning:** Gesture because it is a coordinated typographic register shift (font-family + font-style) that changes HOW the reader processes the text. The same content in Inter would read as editorial commentary; in Instrument Serif italic it reads as voice-of-the-system.

**Multi-channel coordination:** Font-family shift (body -> display) + font-style (normal -> italic). Two properties shift together to create the "Archivist voice."

**Pipeline implication:** "What if philosophical content used a different typographic voice from technical content — so the reader could hear the system speaking?"

**Derivation question:** "What if the design system had a distinct typographic voice for its deepest principles — serif italic as the voice of the Archivist?"

---

## METACOGNITIVE CHECK

1. **Did I read the ENTIRE file (CSS + HTML)?** Yes. I read all 1484 lines across 3 reading passes: lines 1-500, 500-1000, 1000-1484. I covered the locked layer CSS (70-484), the axis layer CSS (543-963), and the full HTML body (966-1484).

2. **Am I identifying elements at the DOM level?** Yes. Every entry maps to specific CSS classes and HTML line numbers. I identified DOM elements (`.spiral-map`, `.confidence-badge`, `.stratum`), pseudo-elements (`::before`, `::selection`), and media queries (`@media (max-width: 769px)`).

3. **Could I have missed subtle gestures?** I checked for: pseudo-elements (found `::selection` and `.open-questions li::before`), media queries (found responsive collapse at 769px and reduced-motion at line 434), inline style overrides (found amber border on Open pull quote at line 1334), and multi-property CSS shifts across strata. The reduced-motion media query (line 434) could be considered a prophylactic gesture but I excluded it as accessibility infrastructure rather than a reader-experience shift. The print styles (lines 445-468) are similarly infrastructure.

4. **Am I applying the reader-function test honestly?** Yes. I excluded: the skip-link (accessibility infrastructure), the `.sr-only` class (screen reader utility), the global reset (technical plumbing), base typography rules (foundation, not gestures), and print styles. Each included element passes the removal test — removing it would change the reader's experience of orientation, confidence awareness, or reading register.

5. **Did I check pseudo-elements?** Yes. Found: `::selection` (line 209), `.open-questions li::before` (line 846). Both cataloged. No `::first-letter` in this file (unlike OD-006). No significant `::after` pseudo-elements.

6. **Did I check responsive breakpoints?** Yes. Found `@media (max-width: 769px)` at line 861 (cataloged as gesture #15). Also found `@media (prefers-reduced-motion: reduce)` at line 434 and `@media print` at line 445 (both excluded as infrastructure).

7. **Am I distinguishing between styling and instruments/gestures?** Yes. I excluded base typography (h1-h6 sizing, body line-height), the global reset, link styles, focus-visible styles, and the `.page-container` max-width as pure styling rather than reader-experience devices. The border-weight gradient is NOT "just styling" — it is a semantic encoding where visual weight IS meaning.

---

## SUMMARY

| Category | Count | Elements |
|----------|-------|----------|
| **Instruments** | 8 | Spiral Map, Confidence Badge, Stratum Transition, Metadata Panel, Meta Line, Version Badge, RAR Block, Page Footer |
| **Gestures** | 8 | Border-Weight Gradient, CRESCENDO Spacing, Background Zone Shift, Typography Scale Degradation, Pull Quote Modulation, Open Question "?" Prefix, Responsive Collapse, Selection Styling, Callout Essence Serif Voice |
| **Components** | 10 | Stratum, Spiral Container, Comparison Grid, Pull Quote, Callout, Open Questions List, Header, Table, Code Block, Inline Code |
| **Total** | **27** | |

**Core thesis of AD-004:** The angular spiral resolves the apparent tension between radial geometry and horizontal geological layering. Border-weight gradient (4px/3px/1px) IS the geological encoding — visual weight directly communicates confidence. The spiral's outward expansion manifests as increasing left-padding, creating CRESCENDO whitespace where the amount of breathing room IS the certainty signal.
